spring:
  lifecycle:
    timeout-per-shutdown-phase: 20s
  task:
    scheduling:
      # @Scheduled 기본은 스레드 1개 — 알림 다이제스트가 오래 돌면 다른 스케줄 작업이 밀린다.
      pool:
        size: 2
  application:
    name: dev-jobs-backend

  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5433/devjobs}
    username: ${DATABASE_USER:devjobs}
    password: ${DATABASE_PASSWORD:devjobs_local}
    hikari:
      # 동시성 부하에서 꼬리 지연(p95/p99) 완화 — 10 은 50 VU 부하에서 커넥션 큐잉 유발.
      # Postgres 기본 max_connections=100 내라 안전. 환경변수로 운영별 조정 가능.
      maximum-pool-size: ${DB_POOL_SIZE:30}

  jpa:
    # 뷰 렌더(JSON 직렬화)까지 DB 커넥션을 붙들지 않도록 OSIV 끔 — 풀(10) 보유시간 단축.
    # 대부분 응답이 native 쿼리+DTO 라 lazy 직렬화 리스크 낮음(JobEntity.company 는 EAGER).
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_batch_fetch_size: 100
        jdbc:
          time_zone: UTC

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  jackson:
    property-naming-strategy: SNAKE_CASE
    default-property-inclusion: non_null

  # Redis (app.ratelimit.redis=true 일 때만 사용). 빈은 항상 생성되지만 lazy 라 미사용 시 연결 안 함.
  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: ${SPRING_DATA_REDIS_PORT:6379}
      # JPA 리포지토리를 Redis 후보로 스캔하는 시작 로그 노이즈 제거 — Redis 리포지토리 미사용(레이트리밋은 템플릿만 씀).
      repositories:
        enabled: false

  mail:
    host: ${MAIL_HOST:}
    port: ${MAIL_PORT:1025}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: ${MAIL_SMTP_AUTH:false}
          starttls:
            enable: ${MAIL_SMTP_STARTTLS:false}

  security:
    oauth2:
      client:
        registration:
          github:
            client-id: ${GITHUB_CLIENT_ID:dummy-client-id}
            client-secret: ${GITHUB_CLIENT_SECRET:dummy-client-secret}
            scope: read:user,user:email
          google:
            client-id: ${GOOGLE_CLIENT_ID:dummy-client-id}
            client-secret: ${GOOGLE_CLIENT_SECRET:dummy-client-secret}
            scope: openid,email,profile

app:
  base-url: ${APP_BASE_URL:http://localhost:3000}
  mail-from: ${MAIL_FROM:no-reply@devpass.local}
  # 운영 배포에서 true 로 설정 — JWT/내부/DB 시크릿이 dev 기본값이면 부팅 거부(fail-fast).
  # 로컬/CI 기본 false(기본값 사용 허용).
  require-secure-secrets: ${APP_REQUIRE_SECURE_SECRETS:false}
  # 분석 요약(/api/v1/analytics/summary) 접근 허용 이메일(쉼표 구분). 미설정 시 아무도 못 봄.
  admin-emails: ${ADMIN_EMAILS:}
  ratelimit:
    # NL 추천 IP당 고정창 한도. 기본 10/시간(운영 LLM 비용 가드). 로컬 개발은 bootRun 이
    # NL_RATELIMIT_CAPACITY=100 으로 올린다(build.gradle). 운영은 jar 로 띄워 env 미설정 → 10 유지.
    nl-capacity: ${NL_RATELIMIT_CAPACITY:10}
    nl-window-ms: ${NL_RATELIMIT_WINDOW_MS:3600000}
    # 요약(AI 생성=캐시 미스) IP당 고정창 한도. 창은 nl-window-ms(1시간) 공유. 운영 기본 20/시간.
    summary-capacity: ${SUMMARY_RATELIMIT_CAPACITY:20}
    # true 면 RedisRateLimiter(다중 인스턴스 공유 + 재시작 보존), 아니면 인메모리(단일 인스턴스).
    # 운영 다중 인스턴스에서 true + spring.data.redis 설정. 로컬/CI 기본 false.
    redis: ${APP_RATELIMIT_REDIS:false}

auth:
  internal-secret: ${INTERNAL_AUTH_SECRET:dev-internal-secret-change-me}

ai:
  base-url: ${AI_BASE_URL:http://localhost:8001}

# 세션 JWT 서명 시크릿 (HS256, 최소 32바이트). web 미들웨어가 동일 시크릿으로 검증. 운영은 반드시 env 로 교체.
jwt:
  secret: ${JWT_SECRET:dev-local-jwt-secret-change-me-min-32-bytes!!}

server:
  port: ${PORT:8080}
  # 배포/재시작 시 진행 중 요청을 마치고 종료(기본 immediate 는 스트리밍 코치 응답을 끊는다).
  shutdown: graceful
  # 리버스 프록시(운영) 뒤에서 X-Forwarded-* 를 해석 — rate limit 의 클라이언트 IP 인식 전제.
  forward-headers-strategy: framework
  # 응답 gzip 압축 — web↔backend(운영은 별도 서비스) 홉의 JSON 페이로드를 줄인다.
  # 브라우저←Next 홉은 Next 가 이미 압축하므로, 이 설정은 backend→Next 구간에 효과.
  compression:
    enabled: true
    mime-types: application/json,application/xml,text/plain,text/css,text/javascript
    min-response-size: 1024

logging:
  level:
    root: INFO
    com.devjobs: DEBUG
